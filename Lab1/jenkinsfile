pipeline{
    agent any
    environment{
        REPO_NAME = "RRSasASD"
        REURL = "http://github.com"
        GITUSER = "alphik"

    }

    stages{
        stage("A"){
            steps{
                echo "========executing A========"
            }
            post{
                always{
                    echo "lab1_1_$REPO_NAME"
                }
                success{
                    echo "========A executed successfully========"
                }
                failure{
                    echo "========A execution failed========"
                }
            }
        }

        stage('Create Dynamic Variables') {
            steps {
                script {
                    env.DYNAMIC_VAR = "DynamicValue_${BUILD_NUMBER}"
                }
            }
        }


        stage('print Dynamic Variables') {
            steps {
            
                echo "$DYNAMIC_VAR"
            }
        }
    }

    post{
        always{
            echo "========always========"
        }
        success{
            echo "========pipeline executed successfully ========"
        }
        failure{
            echo "========pipeline execution failed========"
        }
    }
}